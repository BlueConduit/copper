{
  "name": "copper",
  "private": true,
  "repository": "git@github.com:BlueConduit/copper.git",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.1.0",
    "@commitlint/config-conventional": "^16.0.0",
    "@storybook/addon-docs": "^6.4.18",
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  }
}
